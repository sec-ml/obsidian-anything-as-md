# Test vault

Open this folder as a vault in Obsidian to try the plugin.

From the repo root, run `npm run test-copy` first so that `main.js`, `manifest.json`, and `styles.css` are copied into `.obsidian/plugins/anything-as-md/`. Then enable **Anything as Markdown** under **Settings → Community plugins**.
